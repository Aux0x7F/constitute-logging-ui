# constitute-logging-ui

`constitute-logging-ui` is the browser surface for logging review.

It presents event lists, search, safe facts, logging health, projection
coverage, and review posture without owning event ingestion, indexing, encrypted
detail grants, or detection policy.
